
//Doubling of numbers

let numbers = [1, 2, 3, 4];
let newNumbers = [];

const filterOddNumbers = numbers.filter(number => (newNumbers = number % 2 !== 0)).map(number => (newNumbers = number * 2));
console.log(filterOddNumbers); // [2, 6]
